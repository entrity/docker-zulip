Attachments and images are stored in `/data/uploads/files/...` in the container.

Have a look at `path_id` field in the db table `zerver_imageattachment`, `server_attachment`, and `server_archivedattachment`.